# Tests

Run with `pytest` from the project root.

Smoke tests assert that `sensei init` produces a well-formed instance. Full pedagogical behavioral tests will live here once protocols are authored — see [`docs/sensei-implementation.md`](../docs/sensei-implementation.md).
